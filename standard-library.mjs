import os from "os";

console.info(os.platform());
console.table(os.cpus());



// Cara lama.
const os = require("os");


// Sebelum ada javascript Modules di node js kita melakukan require function.
console.info(os.platform());
console.table(os.cpus());